package components;

/**
 * The interface that every component should implement
 * 
 * The render function should be called at every frame re-render
 */
public interface Renderable {
    public void render();
}